[package]
name = "demo-lambda-axum"
version = "0.1.0"
edition = "2024"

[dependencies]
lambda_http = "0.14.0"

axum = "0.8"
tokio = { version = "1", features = ["macros"] }
tower-http = { version = "0.6", features = ["fs"] }
